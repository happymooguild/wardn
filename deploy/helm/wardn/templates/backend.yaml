apiVersion: apps/v1
kind: Deployment
metadata:
  name: wardn-backend
  labels:
    {{- include "wardn.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        {{- include "wardn.labels" . | nindent 8 }}
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.backend.port }}
          env:
            - name: PORT
              value: {{ .Values.backend.port | quote }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: wardn-secrets
                  key: database-url
            - name: SEED_APPS
              valueFrom:
                secretKeyRef:
                  name: wardn-secrets
                  key: seed-apps
            - name: PUBLIC_BASE_URL
              value: {{ .Values.backend.publicBaseUrl | quote }}
            - name: ALLOW_LOCAL_WEBHOOKS
              value: {{ .Values.backend.allowLocalWebhooks | quote }}
            - name: SIGNOZ_URL
              value: {{ .Values.backend.signozUrl | quote }}
            - name: SIGNOZ_API_KEY
              value: {{ .Values.backend.signozApiKey | quote }}
            - name: SIGNOZ_UI_URL
              value: {{ .Values.backend.signozUiUrl | quote }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.backend.port }}
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.backend.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  # Name is intentionally static: the frontend's nginx.conf proxies to
  # http://wardn-backend:8080, and the sample-app targets the same host.
  name: wardn-backend
  labels:
    {{- include "wardn.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/component: backend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
